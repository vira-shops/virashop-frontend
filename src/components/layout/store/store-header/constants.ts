// Nav items used to be hardcoded here (retail-only) even though both the
// desktop nav and mobile sidebar are shared between storefronts — the
// wholesale header silently linked into /retail/*. Nav content now comes
// from each channel's own config (`@/config/storefront`), passed in as
// `config.navItems` / `config.mobileNavItems`.

export const CATEGORIES_SECTION_TITLE = 'دسته‌بندی';
