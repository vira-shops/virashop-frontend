export { CATALOG_SORT_OPTIONS, SORT_LABEL } from '@/features/catalog/constants';

/** «۱۲۰ محصول» — the result count beside the tabs. */
export const resultCountLabel = (total: string) => `${total} محصول`;
