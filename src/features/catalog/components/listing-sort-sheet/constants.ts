export { CATALOG_SORT_OPTIONS, SHEET_HEADER_CLASS, SORT_LABEL } from '@/features/catalog/constants';

export const SORT_RADIO_NAME = 'catalog-sort';

/** `flex-row-reverse` puts the control on the left of an RTL row, the label on the right. */
export const SORT_ROW_CLASS = 'w-full flex-row-reverse justify-between py-4';
