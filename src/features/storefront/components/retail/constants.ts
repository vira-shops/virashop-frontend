/*
 * Constants shared by several retail sections only — each section keeps the
 * rest in its own `constants.ts`.
 */

/** The hero's scroll-down button targets the promo slider below it. */
export const RETAIL_NEXT_SECTION_ID = 'promo-slider';
