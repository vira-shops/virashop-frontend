/** Seller thumbnails shown on an invoice card before the «+N» counter. */
export const INVOICE_THUMBNAILS = 3;

/** 48px rounded square — the design draws products and the «+N» counter alike. */
export const TILE_CLASS =
  'rounded-4 border-gray-100 relative size-13 shrink-0 overflow-hidden border';

/** The «+N» tile — frosted over the row, trailing the thumbnails on the end side. */
export const MORE_TILE_CLASS =
  'rounded-4 flex size-13 shrink-0 items-center justify-center border border-gray-100 bg-blue-50/80 backdrop-blur-[3.5px]';
