export const PAYMENT_TYPE_LABEL = 'نوع پرداخت';

/**
 * The design puts the picker straight on the page: no surrounding card, the
 * field itself carries the white fill, the Gray/2 hairline and the 16px radius.
 */
export const PAYMENT_SELECT_CLASS = 'rounded-8 h-13 border-gray-100 bg-white px-10';
